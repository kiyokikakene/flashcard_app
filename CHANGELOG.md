# 更新履歴

各項目の「影響」は、他のデッキ利用者（友達など）が対応を必要とするかどうかの目安。

- **フロントのみ**: pushすれば自動で全員に反映。対応不要。
- **要Code.gs更新**: 各利用者が自分の`Code.gs`を貼り替えて再デプロイする必要あり。
- **要シート更新**: 上記に加えて、各利用者が自分のスプレッドシートの列構成なども手動で直す必要あり。

## 2026-08-26（2）
- CSVインポート時、front/back/category/noteが完全に一致する問題は重複として自動スキップするように変更。インポート結果のトースト表示も「N件インポートしました（重複M件はスキップ）」のように実際の件数を表示
  - 影響: **要Code.gs更新**。`addQuestions`が返すレスポンス形式が変わる（`{ok:true}`→`{ok:true, added, skipped}`）ため、更新しない場合もインポート自体は動くがトーストの件数表示が`undefined件`になる
  - 注意: この重複チェックはCSVインポート経由のみ。スプレッドシートを直接編集して行を追加する場合は対象外

## 2026-08-26
- 単語帳モードの裏面表示を「代表解1つ」に簡略化。別解（`/`区切り）を全部並べず、各`|`スロットの先頭の値だけを見せるように（例: `東京/とうきょう/トウキョウ`なら「東京」のみ表示）。採点用の判定ロジックは変わらず全別解を見るので、単語帳では別解を教えないが問題モードでは引き続き別解も正解扱いになる
  - 影響: **フロントのみ**

## 2026-08-16
- 問題ごとに例文・解説を登録できる`note`列を追加。単語帳モードは裏面を開いた時、問題モードは採点後に表示
  - 影響: **要シート更新**。`Questions`シートのヘッダー行に`note`列を追加し、`Code.gs`を再デプロイする必要あり（無くても既存機能は壊れないが、noteを書いても表示されない）
- `back`列に`/`区切りで別解（表記ゆれ）を明示登録できる機能を追加（例: `東京/とうきょう/トウキョウ`）。漢字↔読みの自動変換は辞書サイズ（圧縮約18MB）と精度の兼ね合いで見送り、手動登録方式を採用
  - 影響: **フロントのみ**（既存の`back`列データに`/`を含まないものは今まで通り動く）
- 解答の表記ゆれ対応（ひらがな/カタカナの違いを同一視して採点。例: 「とうきょう」と「トウキョウ」を同一視）
  - 影響: **フロントのみ**

## 2026-08-12
- マークをトグル方式に変更（同じマークを再度押すとオン/オフを切り替えられるように。以前は押すたびに記録が増えるログ方式だった）
  - 影響: **要Code.gs更新**（`addMark`→`toggleMark`）。旧方式で溜まった`Marks`シートの重複行が気になる場合は手動でクリア。
- README.mdを利用者向けに書き直し、開発者向け手順をSETUP_DEVELOPER.mdに分離
  - 影響: **フロントのみ**（ドキュメントのみの変更）

## 2026-08-12
- type列（単語/問題の固定区分）を廃止し、同じ問題プールを単語帳モード・問題モードの両方で使えるように変更。モードは学習セッション開始時に毎回選択する方式に
  - 影響: **要シート更新**。`Questions`シートの`type`列を削除（またはシート自体を作り直し）、`Code.gs`を再デプロイ。CSVを入れ直す場合は`type`列なしの新しい`sample.csv`を使用。

## 2026-08-12
- 初期リリース（単語帳モード、問題モード、マーク機能、CSVインポート、複数デッキ管理、統計画面）
