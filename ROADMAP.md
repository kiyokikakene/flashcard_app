# 今後の更新予定

実装するかどうか未確定の案を置いておく場所。開発者向け（[SETUP_DEVELOPER.md](SETUP_DEVELOPER.md)の関連ドキュメント）。

## 保留中

### 問題・解説への画像/音声対応
- 2026-08-16時点で検討開始、実装は保留
- 案A（推奨）: `image` / `audio` 列を追加し、URLを入れてもらって`<img>`/`<audio>`として表示する方式。バックエンド変更はほぼ不要（`note`と同じ扱いでヘッダーを動的に読める）。ただしファイルのホスティングを別途用意する必要がある（Googleドライブの場合は直リンク用URL形式への変換が必要）
- 案B: Apps Script側にDriveへの直接アップロード機能を作る方式。利用者側の手間は減るが、新しいAPI・アップロードUI・payloadサイズの扱いなど実装コストが高い
- 方向性: まず案Aで試して、手間が気になるようなら案Bを検討

### 問題モードの出題方向（表→裏 / 裏→表）
- 2026-08-26時点で検討開始、実装は保留
- 難易度: 中
- 案: クイズ開始時に出題方向（表→裏 / 裏→表 / ランダム）を選べるトグルを追加する。裏→表の場合は `front` を正解として採点することになるが、現状 `front` は単一文字列前提で `back` のような `|`（複数解答）・`/`（別解）構文を持たない。front側にも同じ構文を導入して表記ゆれ・複数解答に対応させるか、front側は完全一致判定のみで妥協するかは要検討
- 影響: フロントのみ（UI・グレーディング分岐の追加）。front側にも別解構文を持たせる場合はCSVフォーマット拡張が絡む

### 順番通りの解答が必須な問題
- 2026-08-26時点で検討開始、実装は保留
- 難易度: 中〜高
- 現状の`|`区切りは順不同のAND判定（どの順で答えても複数解答が全部埋まれば○）。順序を強制するには問題ごとに「順序必須」フラグが必要（例: `back`列に特別な記法を追加する、またはシートに新しい列を足す）。フラグが立っている問題は`gradeQuiz()`内で位置ごとの突き合わせ（`given[i]`と`required[i]`の別解群を照合）に切り替えるロジックを別途実装する必要がある。部分点（`partial`）の扱いをどうするか（順序が一部崩れた場合にどこまで許容するか）も設計判断が要る
- 影響: 要シート更新（新しい列か記法を使う場合）＋フロント側のグレーディングロジック変更

## 実装済み（詳細は[CHANGELOG.md](CHANGELOG.md)）
- 解答の表記ゆれ対応（ひらがな/カタカナ、`/`区切りの別解登録）
- 例文・解説（`note`列）
- 単語帳表示の代表解1つ化（別解は問題モードの採点でのみ使う）
